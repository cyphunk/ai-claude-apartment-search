requests>=2.31
# PIN, do not float: the Playwright version must equal the base-image tag in the
# Dockerfile (mcr.microsoft.com/playwright/python:vX.Y.Z-jammy). The image ships the
# matching Chromium build; a floating "playwright>=..." lets pip install a newer
# Playwright whose Chromium build isn't in the image, breaking every launch. Bump
# this and the Dockerfile FROM together, never one alone.
playwright==1.61.0
